library = "/path/to/library"
old_dir = "/path/to/old"
# Optional local disk for in-progress downloads (recommended when the library is on SMB/NFS).
# If unset, the system temp directory is used.
# cookies = "cookies.txt"
